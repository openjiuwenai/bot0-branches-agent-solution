Web search is rate-limited (HTTP {code}). Continue with other steps first, then retry this same query once later; if it still fails, finish using what you have already collected and state what is missing.
