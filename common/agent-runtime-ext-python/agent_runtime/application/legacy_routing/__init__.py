# coding: utf-8

"""存量编排层的任务态与路由判定。

## 为什么整族落在应用层

**判定要读任务态**——那是经端口的读，不是纯计算，故进不了领域层；
但它也不碰外部世界的具体实现（不认识 Redis、不认识 HTTP），故不该落适配层。
根设计给的切口是「碰不碰外部世界」，这一族落在切口的内侧。

**值对象同样不进领域层**：根设计把领域层定为封闭清单——三个权威数据对象
加任务状态机。归一事件、路由上下文、路由目标是路由这一侧的数据载体，
不是权威领域对象。
"""
