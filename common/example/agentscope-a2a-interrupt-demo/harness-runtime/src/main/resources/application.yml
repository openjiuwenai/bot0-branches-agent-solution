server:
  port: ${HARNESS_AGENT_PORT:18111}

spring:
  application:
    name: agentscope-harness-a2a-demo

agentscope:
  demo:
    api-key: ${DEEPSEEK_API_KEY:}
    base-url: ${DEEPSEEK_BASE_URL:https://api.deepseek.com}
    model: ${DEEPSEEK_MODEL:deepseek-chat}
    workspace: ${AGENTSCOPE_HARNESS_WORKSPACE:target/agentscope-harness-workspace}

openjiuwen:
  service:
    query:
      legacy-path-enabled: true
      webflux:
        enabled: false
    a2a:
      agent-name: "AgentScopeHarnessAgent"
      agent-description: "AgentScope HarnessAgent with A2A HITL confirmation"
      provider-organization: "OpenJiuwen"
      streaming: true
      default-input-modes: ["text", "text/plain"]
      default-output-modes: ["text", "text/plain"]
      skills:
        - id: "approved_transfer"
          name: "Approved Transfer"
          description: "Execute a transfer after explicit structured approval"
          tags: ["agentscope", "harness", "hitl", "transfer"]
          examples: ["Transfer 5 CNY to Li Ming"]
