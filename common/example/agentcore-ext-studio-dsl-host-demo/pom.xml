<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.openjiuwen.example</groupId>
    <artifactId>agentcore-ext-studio-dsl-host-demo</artifactId>
    <version>0.1.0</version>
    <packaging>jar</packaging>

    <name>agentcore-ext-studio-dsl-host-demo</name>
    <description>
        Fake host demo for agent-core-ext-studio-dsl (FEAT-031): hand-built AssembledWorkflow,
        no DSL loader / HTTP. Run after installing agent-core-ext-java locally.
    </description>

    <properties>
        <java.version>17</java.version>
        <maven.compiler.release>17</maven.compiler.release>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <agent-core-ext-studio-dsl.version>0.1.0</agent-core-ext-studio-dsl.version>
    </properties>

    <dependencies>
        <dependency>
            <groupId>com.openjiuwen</groupId>
            <artifactId>agent-core-ext-studio-dsl</artifactId>
            <version>${agent-core-ext-studio-dsl.version}</version>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.13.0</version>
                <configuration>
                    <release>${maven.compiler.release}</release>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>exec-maven-plugin</artifactId>
                <version>3.5.0</version>
                <configuration>
                    <mainClass>com.openjiuwen.example.studio.dsl.host.FakeHostMain</mainClass>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
