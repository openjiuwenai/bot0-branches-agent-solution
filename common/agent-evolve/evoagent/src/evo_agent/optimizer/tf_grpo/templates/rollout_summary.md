请总结该技能文档变体的一次 rollout，供后续组内相对比较使用。

**变体 id：** {variant_id}
**平均分：** {mean_score}（共 {n_cases} 条用例）

**技能变体（SKILL.md）：**
{skill_content}

**各用例结果：**
{cases_text}

请写一份约 **3–5 条**要点的极简摘要，**只服务一个核心目标：说清「期望结果」与「实际预测/输出」差在哪里、为何差**。

摘要必须包含（按此顺序，能合并则合并，勿凑条数）：
1. **期望 vs 实际差异**：按失败/偏差 case id 写清关键偏差；全对则一句「均与期望一致」
2. **差异归因**：更可能由 SKILL 哪类规则缺口/误导导致（点到具体倾向即可）
3. **可选保留点**：仅当有明显一致成功时，用一条写可保留的有效指导；否则省略

约束：
- 不得编造所给输出之外的 case 事实
- 不要复述整份 SKILL、不要堆无关文档结构说明
- 只输出摘要正文（不要前言）

Rollout 摘要：
