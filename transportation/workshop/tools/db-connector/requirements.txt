# db-connector 依赖
# 安装：pip install -r requirements.txt

# 核心
SQLAlchemy>=2.0
PyMySQL>=1.1
psycopg[binary]>=3.1
pydantic>=2.5
pydantic-settings>=2.1
PyYAML>=6.0

# HTTP 服务（FastAPI）
fastapi>=0.110
uvicorn>=0.27

# MCP 服务（workshop/mcp/db-connector-server 使用）
mcp>=1.0

# 凭证管理（Vault 模式，按需启用）
hvac>=2.0

# 本地加密配置（按需启用）
cryptography>=42.0

# 测试
pytest>=7.0
pytest-asyncio>=0.23
